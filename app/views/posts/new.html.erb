投稿の新規登録

<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.text_area :name, placeholder: "新しい投稿のタイトル" %>
  <%= f.text_area :content, placeholder: "新しい投稿の内容" %>

  <%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>